#include <stdlib.h>
#include <stdio.h>

#include "UNO.h"

int main(){
    jeu game;
    t_main main_A, main_B;
    carte last;
    
    int retour = preparer_jeu_UNO(le_jeu, 7, &main_A, &main_B, &last);
    return EXIT_SUCCESS;
}